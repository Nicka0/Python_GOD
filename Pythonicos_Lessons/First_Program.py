# Using raw_input for catch user's input.
#ip = raw_input('Digite um endereco IP: ')
#print 'O endereco  '+ ip + ' e um endereco valido'

# Printing a variable. In this case,ip. %s Used to show a value without putting it in the text.
#print 'O endereco %s e um endereco valido' % ip

#Counting type int, using %d (for integer) and Len() on counting.
#print 'O endereco %s tem um tamanho de %d caracteres' %(ip,len(ip))

#Convert counting in type string. Use the variable str() for conversion.
#print 'O endereco %s tem um tamanho de %s caracteres' %(ip,str(len(ip)))

#Counting float numbers, using %f. To show 2 numbers after comma, use .n before the f (n = number)
#print 'Numero quebrado %.2f ' %1.345678
